services:
  frontend:
    build: ./TTT-Web-Front/
    depends_on:
      - backend
    ports:
      - "11451:80"
  backend:
    depends_on:
      - redis
    build: ./TTT-Web-Back/
    ports:
      - "19198:8080"
  redis:
    image: redis:alpine